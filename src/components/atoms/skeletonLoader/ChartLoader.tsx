import { Skeleton } from "@/components/ui/skeleton";

const ChartLoader = () => {
  return (
    <Skeleton className="h-[300px] max-w-lg w-full rounded-lg bg-gray-300" />
  );
};

export default ChartLoader;
